# JetBot 進度紀錄

> 最後更新：2026-06-05

## 本週完成

- ✅ Jetson 透過 Micro-USB 連上 Mac（USB Device Mode）
  - Mac 端：`192.168.55.100` ↔ Jetson 端：`192.168.55.1`
- ✅ SSH 登入成功（帳密 `jetbot` / `jetbot`）
- ✅ Jetson WiFi 連線設定（`nmcli`，需 `sudo`）
  - 已連 `iPhone (4)`，IP `172.20.10.14`，已存檔、開機自動重連
- ✅ 確認軟體環境：SD 卡映像版 `jetbot 0.3.0`（**非 docker**）
- ✅ JupyterLab 正常運作於 `:8888`（密碼 `jetbot`）
- ✅ 攝影機確認：用 **Logitech C270 USB**（`/dev/video0`），**非** CSI
  - 註：板上 CSI 相機 IMX219 偵測失敗（error -121），但本專案不使用它
- ✅ 重開機驗證：WiFi 自動重連、C270 自動偵測，皆正常

## 連線方式

| 用途 | 指令 / 網址 |
|------|------|
| SSH（USB，推薦）| `ssh jetbot@192.168.55.1` |
| SSH（WiFi）| `ssh jetbot@172.20.10.14` |
| JupyterLab | `http://192.168.55.1:8888`（密碼 `jetbot`）|

> host key 變更導致 SSH 被擋時：`ssh-keygen -R 192.168.55.1` 後重連。

## 待辦清單（已排除「避免碰撞」單獨任務）

目標：**走完整學習流程**（自己收資料 → 訓練 → TensorRT → 即時展示）

| # | 任務 | 需攝影機 | 狀態 | 素材路徑 |
|---|------|:---:|------|------|
| 1 | Basic Motion 基本動作 | ❌ | 待做 | `~/Notebooks/basic_motion` |
| 2 | Teleoperation 遙控 | ✅ | 待做 | `~/Notebooks/teleoperation` |
| 3 | Road Following 道路跟隨（全流程）| ✅ | 待做 | `~/jetbot_CAVEDU/road_following`（用 **C270 版** notebook）|
| 4 | 結合：道路跟隨+防撞 | ✅ | 待做 | `~/CAVEDU_advanced/Jetbot_Road_Following_and_Collision_Avoidance`（完整）|
| 5 | *(stretch)* Object Following 物體跟隨 | ✅ | 缺 ssd engine | `~/jetbot/notebooks/object_following`（只有 live_demo.ipynb）|

## 重要決策與註記

- **攝影機 = C270 USB**：所有 notebook 用 `_C270` 變體（如 `data_collection_C270.ipynb`、`live_demo_C270.ipynb`）。
- **沒有遊戲手把**：Teleoperation 改用 Jupyter widget 按鈕/滑桿控制 + 影像串流 + 存快照。
- **#4 / #5 的防撞依賴**：沿用 `~/jetbot_CAVEDU/collision_avoidance` 內現有訓練好的 `.pth`，不重新收資料/訓練。
- **賽道**：已有實體賽道可收資料。
- **#5 物體跟隨**列為最後 stretch：缺 `ssd_mobilenet_v2_coco.engine`（與 JetPack 版本綁定），有時間再下載/build。

## 現有可沿用的模型 / 資料

- 道路跟隨模型：`~/jetbot_CAVEDU/road_following/best_steering_model_xy*.pth`（多版本）
- 防撞模型：`~/jetbot_CAVEDU/collision_avoidance/best_model_*.pth`（多版本）
- 既有資料集：`~/jetbot_CAVEDU/*/[*_dataset_*]`

## 操作注意事項

- Basic Motion 等任務馬達會動，先確保 JetBot 有足夠移動空間。
- 收/訓練資料時，每跑完一個 notebook 要 **Shutdown All Kernels**，避免攝影機被佔用。
- 關機用 `sudo shutdown -h now`，等指示燈熄滅再拔電源（保護 SD 卡）。
- iPhone 熱點 IP（`172.20.10.x`）每次重開熱點可能變動；長期使用建議改連固定 WiFi。

## 下一步

從 **#1 Basic Motion** 開始（不需攝影機，可先驗證馬達）。
