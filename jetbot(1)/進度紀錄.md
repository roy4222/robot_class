# JetBot 進度紀錄

> 最後更新：2026-06-15

## 現況總結
**道路跟隨（Road Following）端到端跑通** —— 自製 pipeline（收資料 → 訓練 → demo），
車能在白紙粉紅線賽道上自走、跟線。完整說明見
[`jetson/road_following/README.md`](../jetson/road_following/README.md)。

## 2026-06-15 今日進度
- ✅ 部署自製三支腳本到 Jetson（`jetbot_web.py` / `train_rf.py` / `road_follow.py`）；
  經 ultracode 稽核修好：座標格式（0–223）、BGR/RGB 通道一致、水平翻轉增強、資料防呆。
- ✅ 馬達、C270 相機、免密碼 SSH、無線連線全通。
- ✅ 收 277 張**平衡**資料（左 73 / 中 34 / 右 170），在 **Mac M1 MPS** 訓練（test loss ~0.023），模型送回 Jetson demo。
- ✅ 解掉走走停停：`jetbot_stats` 服務 disable + `--min-motor` 馬達死區補償。
- ✅ 解掉 torch 存檔格式 Mac↔Jetson 不相容。
- 🔄 循跡精準度（走歪）持續靠補資料改善。

## 連線
| 用途 | 指令 / 網址 |
|------|------|
| SSH（USB，最穩）| `ssh jetbot@192.168.55.1`（已設免密碼金鑰）|
| SSH（WiFi）| `ssh jetbot-wifi`（IP 隨 iPhone 熱點變動）|
| 收資料網頁 | `http://192.168.55.1:5000`（跑 `jetbot_web.py` 後）|
| JupyterLab | `http://192.168.55.1:8888`（密碼 `jetbot`）|

## 環境備忘
- 相機 C270 USB `/dev/video0`；馬達板 DFRobot I2C `0x10`；DC 馬達啟動門檻 ~0.18。
- **`jetbot_stats.service` 已 disable**（會搶 CPU 造成 demo 卡頓，勿開）。
- Jetson 系統時鐘不準（顯示 2023），資料夾時間戳會錯，不影響功能。
- SD 卡映像 `jetbot 0.3.0`（非 docker）；關機 `sudo shutdown -h now`，等燈滅再拔電。

## 五大任務狀態
| # | 任務 | 狀態 |
|---|------|------|
| 1 | Basic Motion 馬達 | ✅ 完成 |
| 2 | Teleoperation 遙控 | ✅ 已併入 `jetbot_web.py`（WASD）|
| 3 | **Road Following 道路跟隨** | ✅ pipeline 跑通，循跡精準度持續優化 |
| 4 | 結合道路跟隨 + 防撞 | ⬜ 未做（非繞圈必要）|
| 5 | Object Following | ⬜ stretch，缺 ssd engine |

## 下一步
- 補「直線 + 走歪點」correction 資料，重訓提升循跡，目標穩定繞完一圈。
- 期末 demo / 心得（06/26 補 demo）。
